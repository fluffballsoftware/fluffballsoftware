---
trigger: always_on
---

Rule [Data Privacy]:

When generating code that handles email, phone, or location, the agent must ensure these fields are treated as PII.

The agent must never suggest logging the values of these fields to CloudWatch.